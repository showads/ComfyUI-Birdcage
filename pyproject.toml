[project]
name = "comfyui-bird-cage"
version = "0.1.0"
description = "Interactive AI scene compiler and standalone UI for ComfyUI"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.ruff]
line-length = 120
